

var otp=Math.floor(Math.random()*10000)
console.log(otp);

//radom()-will generate random number which is ranging from 0 to 1 and then multiplying with 10/100/1000/ to get no
//floor()-will generate valid number